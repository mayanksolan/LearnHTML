let counter = arr => {
  return `There are ${arr.length} elements in this array`;
};
let firstElement = arr => {
  return `The first element of the arr is ${arr[0]}`;
};
let pi = 3.142;

module.exports = { counter, firstElement, pi };
